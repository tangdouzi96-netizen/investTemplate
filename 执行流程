用户："分析 {某股票}"
  │
  ├─→ 查 CLAUDE.md "已分析标的" 列表
  │     ├── 在列表中 → 跳过筛选，直接更新报告
  │     └── 不在列表 → 触发 6 关快速筛选
  │           │
  │           ├── 任一关 ❌ → 告知卡在哪关，等用户决定
  │           └── 全过 ✅ → 继续
  │                 │
  │                 ├─→ readPDF.md §0 前置铁律
  │                 │     └── "任何标的必须先完成数据溯源表"
  │                 │
  │                 ├─→ readPDF.md §1 四步 SOP
  │                 │     ├── ① dump → 定位报表页（搜审计报告→翻4-6页）
  │                 │     ├── ② 批提取 → 标页码+单位 → 平衡验算（不通过→重来）
  │                 │     ├── ③ 交叉验证 → 次年比较列互验
  │                 │     └── ④ 并行WS → 填入真实验证层级（不虚标✅）
  │                 │           │
  │                 │           └── 达标判定（readPDF.md §1 末尾）
  │                 │                 ├── ✅/🔗/📄/📝 ≥ 95% → 继续
  │                 │                 └── < 95% → 回溯补齐，禁止进入报告
  │                 │
  │                 ├─→ 数据准确性保障流程.md
  │                 │     ├── A/B/C区 溯源表结构标准
  │                 │     ├── 行业特有科目扩展
  │                 │     └── 自检清单（16项逐条过）
  │                 │
  │                 └─→ 个股分析标准模版.md
  │                       ├── 模板12章标准结构
  │                       ├── 7大缺口全覆盖
  │                       └── 校验块（数据来源声明+计算过程+交叉验证表）
  │                             │
  │                             └── 输出报告 → 更新 index.md → 完成
  │
  └─→ 高危提醒（贯穿全程，readPDF.md §7）
        ├── 空值科目=0 → 必须 WebSearch 确认
        ├── 单位混用 → 每页检查表头
        ├── BS 合计陷阱 → 精确匹配，排除"流动/非流动"
        ├── 追溯调整 → 标注双口径
        └── 减值>净利润10% → 🔴预警 + 20%安全边际